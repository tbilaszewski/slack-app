{
  "name": "slack-quotes",
  "scripts": {
    "lint": "tslint --project tsconfig.json",
    "build": "tsc",
    "serve": "npm run build && firebase serve --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "8"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tbilaszewski/slack-app.git"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^8.0.0",
    "firebase-functions": "^3.1.0",
    "moment": "^2.24.0"
  },
  "devDependencies": {
    "tslint": "^5.12.0",
    "typescript": "^3.2.2",
    "moment": "^2.24.0"
  },
  "private": false,
  "bugs": {
    "url": "https://github.com/tbilaszewski/slack-app/issues"
  },
  "homepage": "https://github.com/tbilaszewski/slack-app#readme"
}
